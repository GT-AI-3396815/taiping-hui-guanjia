# 太平慧管家（WorkBuddy 迁移 + 专业版优化）

太平保险代理人专属智能事业伙伴 —— 从 Kimi（xyrjweloa4nq4.ok.kimi.link）完整迁移的静态 SPA（React + Vite 构建产物），并已完成一轮面向专业代理人的内容与合规优化。

## 目录结构

```
index.html                          入口（去除 Kimi 埋点、zh-CN、内联 SVG favicon、内联增强层脚本）
assets/index---udaeVd.js            主 JS 包（含多项修复与功能注入，见下）
assets/index-BbKmavjm.css           样式表
assets/__vite-browser-external-BIHI7g3E.js  动态分包（Node API 占位，浏览器端不执行）
images/warm-*.jpg                   4 张页面素材图（hero / meeting / stilllife / texture）
api_push.py                         GitHub Git Data API 推送脚本（git 通道不稳时备用）
```

## 本地运行

任意静态服务器即可，例如：

```bash
cd 本目录
python -m http.server 8917 --bind 127.0.0.1
# 浏览器打开 http://127.0.0.1:8917/
```

## 2026-09-11 专业版优化（8 项）

| # | 类别 | 内容 |
|---|------|------|
| 1 | 合规护栏 | System prompt 强化（禁绝对化用语/收益承诺 + 长文案强制合规尾注）；生成结果区固定免责声明行；输入主题含高风险词（稳赚/保本/零风险等）自动弹出合规提醒 |
| 2 | 生成历史 | 拦截 DeepSeek 请求/响应（兼容 SSE 流式），生成内容自动存入 localStorage（本机最多 100 条），右下角「历史」面板支持查看全文/复制/删除/清空 |
| 3 | 模块改名 | 导航「太平课堂」→「课件模板」、徽标「太平大课堂」→「课件模板库」，消除"有课可学"的预期落差 |
| 4 | 展业锦囊 | 右下角「锦囊」面板：客户异议处理话术 7 条、客户跟进节奏表 6 阶段、产品一句话讲法 5 条（通用合规框架，无编造产品数据） |
| 5 | 额度说明 | 额度用尽文案明确"100 次/天、明日 0 点恢复、历史记录可回看"；按钮短文案同步优化 |
| 6 | 水印升级 | 「上传照片添加水印」先弹高级设置：主水印文字（默认含姓名）/副文字/不透明度 20-100%/位置（底部衬底·居中大字·全图平铺），再选照片生成 |
| 7 | 内容人设 | 右下角「人设」面板：专业稳重/亲和温暖/活力热情，持久化保存并自动注入每次生成请求 |
| 8 | 法律信息 | Footer 新增：站点性质声明（独立演示站，非中国太平官网）+ 免责声明/隐私政策全文弹层 + 版权行 |

其他修复：AI 请求 401/402 错误映射为友好中文提示（原为英文原文直出）。

## 迁移时发现并修复的问题（历史记录）

| # | 问题 | 修复 |
|---|------|------|
| 1 | `images/warm-texture.jpg` 未随迁移抓取，页面 404 | 已从源站补齐全部 4 张图 |
| 2 | Kimi 平台埋点脚本迁出后无效且拖慢加载 | 已移除 |
| 3 | favicon 404 | index.html 内联金色「太」字 SVG 图标 |
| 4 | 【原站真 bug】PPT 下载必失败：`write({type:"blob"})` 传参错误 | 改为 `write("blob")`，实测可下载 .pptx |
| 5 | 【原站真 bug】水印按钮无 onClick（纯摆设） | bundle 内注入完整 canvas 水印实现 |
| 6 | GitHub Pages 子路径白屏：React Router 仅注册 `path:"/"` | 路由改 `path:"*"`；4 处图片绝对路径改相对路径 |

## 回归测试结论（Playwright + Chrome，22/22 PASS）

- 首页渲染 / 导航改名 / 额度用尽新文案：PASS
- AI 生成全链路（请求注入合规与人设指令 / 结果区免责行 / 流式响应解析存历史 / 高风险词提醒）：PASS（Mock 接口）
- 展业锦囊三 Tab / 历史面板查看复制 / 人设切换持久化：PASS
- 水印高级设置弹窗 + 自定义文字/透明度/位置生成下载：PASS
- PPT 生成并下载：PASS
- Footer 法律信息 + 免责声明弹层：PASS
- 子路径环境（模拟 GitHub Pages /taiping-hui-guanjia/）渲染完整、0 控制台错误：PASS

## ⚠️ 已知风险与待办

1. **DeepSeek API Key 已双双失效**（旧 sk-9c24… 已作废；新 hi-68fy… 提示 invalid，疑似额度耗尽或被回收，2026-09-11 实测 401/402）。**需到 DeepSeek 后台确认并更换有效 Key**，替换位置：`assets/index---udaeVd.js` 内 `_3` 常量。正式对外前建议改为后端代理转发，前端不落 Key。
2. AI 直连 `api.deepseek.com`，浏览器跨域依赖对方 CORS 策略。
3. 「今日额度」（100 次/天）仅存于前端本地，清缓存即可绕过，无实际限制力。
4. 历史记录存于本机 localStorage，清除浏览器数据会丢失（产品设计取向：不收集用户数据）。
